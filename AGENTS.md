# Project Instructions & Style Guide

- **No Em Dashes**: Never use em dashes (`—`) or en dashes (`–`) anywhere in the application copy, UI text, documentation, or code. Use standard hyphens (`-`), colons (`:`), or commas (`,`) instead.
